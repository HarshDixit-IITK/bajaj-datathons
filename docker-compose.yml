version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - API_HOST=0.0.0.0
      - API_PORT=8000
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OCR_ENGINE=easyocr
    volumes:
      - ./app:/app/app
    restart: unless-stopped

